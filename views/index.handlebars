<h1>Eat Da Burger!</h1>

<h2>Burgerss that are not devoured!</h2>

<ul>
  {{#each burgers}} {{#unless devoured}} {{> burgers devoured=false}} {{/unless}} {{/each}}
</ul>

<h2>Burgers that are devoured!</h2>

<ul>
  {{#each burgers}} {{#if devoured}} {{> burgers devoured=true}} {{/if}} {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bgr">Burger Name:</label>
    <input type="text" id="bgr" name="name">
  </div>

  <div class="form-group">
    <label for="devour">Devoured?</label>
    <br>
    <input type="radio" name="devour" value="1" checked> Devoured!
    <br>
    <input type="radio" name="devour" value="0"> Not devoured
  </div>

  <button type="submit">Add Burger</button>
</form>