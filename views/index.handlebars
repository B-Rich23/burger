  <div>
    <img src="/assets/img/yummy-burger.jpg" alt="burger image">
    <h2>Eat-Da-Burger!</h2>
    <ul>
      {{#each burgers}} {{#unless devoured}}  {{/unless}} {{/each}}
    </ul>
        
    <ul>
      {{#each burgers}} {{#if devoured}} {{/if}} {{/each}}
    </ul>

    <form class="create-form">

      <div class="form-group">
        <input type="text" id="bgr" name="name">
      </div>

      <!-- <div class="form-group">
                <label for="devour">Devoured?</label>
                <br>
                <input type="radio" name="devour" value="1" checked> Devoured!
                <br>
                <input type="radio" name="devour" value="0"> Not devoured
            </div> -->

      <button type="submit">Submit</button>



    </form>

          <div>
            <br>
            <p>Click "Submit" to add Burger</p>
            <br>
          </div>

                          {{!-- <label for="bgr">Click "Submit" to add Burger</label> --}}

  </div>
